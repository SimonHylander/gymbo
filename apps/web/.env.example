VITE_API_BASE_URL=
VITE_CONVEX_URL=
VITE_CONVEX_SITE_URL=
VITE_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Principal adapter of the linked Convex deployment ("dev" or "clerk"). Server-owned
# and fail-closed; the authoritative copy is set on the deployment itself:
#   cd packages/backend && bunx convex env set AUTH_PROVIDER dev
AUTH_PROVIDER=
