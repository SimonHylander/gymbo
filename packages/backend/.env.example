# Deployment used by `convex dev` (see `npx convex dev --configure`)
CONVEX_DEPLOYMENT=

# Principal adapter: "dev" or "clerk". Fail-closed — unset or any other value
# makes every function raise. The authoritative copy lives on the deployment:
#   bunx convex env set AUTH_PROVIDER dev
AUTH_PROVIDER=dev
